<div class="app-container">
  {{> sidebar }}
  <div class="detail-container">
    <h1>{{title}}</h1>
    <p>Welcome to {{title}}. Inventory management made easy, so that you can focus on growing your business. 😉</p>
    {{#if error}}
      <p>Error getting dynamic content.</p>
    {{else}}
      <p>Your inventory has the following record counts:</p>

      <ul class="dynamic-info">
        <li><strong>Categories</strong>:{{data.category_count}}</li>
        <li><strong>Boards</strong>: {{data.board_count}}</li>
      </ul>
    {{/if}}
  </div>
</div>